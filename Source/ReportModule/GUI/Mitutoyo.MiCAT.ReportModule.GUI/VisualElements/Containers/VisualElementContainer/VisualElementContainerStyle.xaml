<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:l="clr-namespace:Mitutoyo.MiCAT.ReportModule.GUI.VisualElements.Containers.VisualElementContainer">
   <Style TargetType="l:VisualElementContainer">
      <Setter Property="Template">
         <Setter.Value>
            <ControlTemplate>
               <ContentPresenter
                  Content="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}"
                  Width="{Binding Path=VMPlacement.VisualWidth}"
                  Height="{Binding Path=VMPlacement.VisualHeight}"
               />
            </ControlTemplate>
         </Setter.Value>
      </Setter>
   </Style>
</ResourceDictionary>